class InstallationModel{
  String Product;
  int days;
  dynamic amount;
  String covered;
  String notCovered;

  InstallationModel({
      required this.Product,required this.days,required this.amount,required this.covered,required this.notCovered});
}