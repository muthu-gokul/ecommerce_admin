class DeliveryChargeModel{
  dynamic from;
  dynamic to;
  dynamic amount;
  String createdAt;
  DeliveryChargeModel({required this.amount,required this.to,required this.from,required this.createdAt});
}